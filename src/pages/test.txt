<button onClick={commentProduct} style={{ color: "black" }}>
              Review
            </button>

            <Typography id="modal-modal-description" sx={{ mt: 2 }}>
            {oneProduct ? (
              <>
                {oneProduct.comments.map((elem) => (
                  <>
                    <Tooltip title="Profile">
                      <IconButton sx={{ paddingLeft: "2px" }}>
                        <Avatar alt={user} src="..." />
                      </IconButton>
                    </Tooltip>
                    <h2>{user}</h2>
                    <h2 style={{ color: "black" }}>{elem.item}</h2>
                    <button
                      onClick={() => deleteComment(elem.id)}
                      style={{ background: "black", color: "white" }}
                    >
                      Delete
                    </button>
                  </>
                ))}
              </>
            ) : (
              <h1>Error</h1>
            )}
          </Typography>



          <form>
        <input type="text" />

        <label htmlFor="">New Username</label>
        <input onChange={handleInp} type="text" />
      </form>
      <button onClick={patchUsername}> Putch </button>